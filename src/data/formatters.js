export const code = (text) => `<pre><code>${text}</code></pre>`;
export const tag = (text) => `<span class="tag inline-block [font:11px_ui-monospace,monospace] tracking-[0.5px] bg-soft rounded-[4px] text-blue py-[6px] px-[10px] border border-solid border-[#d5dfe8]">${text}</span>`;
